name: CI

on:
  push:
    branches:
    - master
    - develop
    
jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master
    - name: composer install
      run: composer install
